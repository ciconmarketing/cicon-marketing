---
import type { HeroData } from '../lib/types';
import HeroClient from './react/HeroClient.tsx';
interface Props { data: HeroData }
const { data } = Astro.props;
---

<HeroClient data={data} client:load />
